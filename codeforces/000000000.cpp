#include <bits/stdc++.h>
using namespace std;
constexpr auto endline = '\n';

auto process() -> void {}
auto solve() -> void {}

auto main() -> int {
    ios::sync_with_stdio(false);
    cin.tie(0);
    process();
    auto tests = 0;
    cin >> tests;
    while(tests--) {
        solve();
    }
    return 0;
}